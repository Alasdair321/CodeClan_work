Task: 20 minutes
Declare a named

function that takes an array of numbers, and returns the sum, or total, of all of the numbers in the array.

Define an anonymous

function expression that takes two arguments:

  an object,
  for example, {
    name: 'Wojtek',
    age: 30
  }
a string,
for exmaple, 'name'
Your

function should
return true
if the given string is a key on the given object and false
if the object does not have a key that matches the string.Store this

function in an appropriately named variable to invoke it.
